{
    "_comment": "Copy to config/credentials.local.json (gitignored). Use nested api.* so C++ merge_patch overlays api.rest_endpoint and keys. Defaults below point at AX PRODUCTION (gateway.architect.exchange) — real funds. To use the sandbox (paper money) instead, swap the host to gateway.sandbox.architect.exchange and use sandbox-issued keys.",
    "api": {
        "rest_endpoint": "https://gateway.architect.exchange/api",
        "ws_endpoint": "wss://gateway.architect.exchange/orders/ws",
        "api_key": "YOUR_API_KEY_HERE",
        "api_secret": "YOUR_API_SECRET_HERE"
    }
}
